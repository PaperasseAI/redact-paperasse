[package]
name = "redact-paperasse-recognizers"
description = "Fast, deterministic regex+checksum PII recognizers (Tier A) — no ML, no network"
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

[lib]
name = "redact_paperasse_recognizers"

[dependencies]
regex = { workspace = true }
