[package]
name = "paperasse-privacy-cli"
description = "CLI for paperasse-privacy: redact PII from images, PDFs, and text"
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

[[bin]]
name = "ppr"
path = "src/main.rs"

[features]
default = []
# Off by default so `cargo build`/`cargo install` for the plain CLI never
# pulls in reqwest — matches paperasse-privacy-core's own tier-b feature
# being opt-in. Build with `--features tier-b` for the `--tier-b` flag.
tier-b = ["paperasse-privacy-core/tier-b"]

[dependencies]
paperasse-privacy-core = { workspace = true }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "fs"] }
